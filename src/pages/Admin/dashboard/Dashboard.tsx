import React from 'react';
import AdminWrapper from "../layouts/AdminWrapper";

const Dashboard = () => {
    return (
        <AdminWrapper>
            admin
        </AdminWrapper>
    );
};

export default Dashboard;
